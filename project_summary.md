# 修士研究 Simulation 部分：项目总结

> 本文档汇总了多 AUV 协调探查系统中，修士阶段 learned path planner（即"用 RL 学习路径规划策略"）simulation 部分的全部设计决策。文档定位为后续模块化架构与代码开发的唯一基准。所有设定均经过多轮讨论锁定；带"⚠"标记处为有意为之的简化或需在论文中谨慎措辞的点。

---

## 1. 项目定位与"一套 sim、一个开关"原则

本 simulation 不是为修士单独编写的退化版，而是**面向博士满血版设计、修士退化版作为其特例**的统一仿真。两版之间唯一的区别由一个开关控制：

- **开关 = 信息环是否闭合 = 观测结果是否回写目標分布確率マップ。**
- **修士（退化，开关 OFF）**：agent 对外生状态（Leader 进度、Follower 位置/电量/承诺队列）反应，但**地图永不被观测结果改写**；优化目标是**先验期望熵降**（基于静态先验 + 传感器模型计算互信息，不需真实测量结果）。
- **博士（满血，开关 ON）**：真实观测结果回写贝叶斯后验 → 期望增益地形改变 → 触发重规划与 MBES 虚警（false-positive）修正。

修士/博士 的边界**只此一条**。代码中该开关必须从一开始预留。DC1 申请书"四项目"结构中的项目③（双向声学反馈做贝叶斯地图修正）即对应此开关的打开。

⚠ 工作量提示：修士版 = 严格 N=2 + 滚动反应式（方案A），本质已是一个相当完整的多机器人滚动时域 informative planning，真正被"退化"掉的只有信息回写。"修士=最小可行版"这一旧定位，在本设计下实际偏重。这是为"sim 贴合满血版"主动付出的代价，已接受。

---

## 2. 术语对照（已锁定）

| 中/英概念 | 本项目固定术语 | 说明 |
|---|---|---|
| Leader 用声呐（MBES·SSS）做初步调查 | **目標探査** | 对应过去的 acoustic scanning |
| Follower 用相机做进一步确认 | **目標観測** | 对应过去的 optical confirmation；过去文档曾用"確認"，现全文统一为"観測" |
| 二维目标概率分布图 | **目標分布確率マップ** | 不另造新词 |

---

## 3. 世界与几何设定

- **概率场 p(x,y)**：世界坐标系下的静态二维场，**graded（分级连续概率，非二值检测掩膜）**。由离线数据处理一次性生成，修士版 episode 内**永不更新**。
- **物理尺度**：200 m × 100 m。
- **分辨率**：0.2 m ⇒ 栅格 1000 × 500 = 50 万 cell。此高分辨率**仅用于价值结算、走廊覆盖、能耗几何**，**不是 RL 的输入/动作分辨率**。
- **Leader 轨迹**：预设 lawnmower（割草机往复测线），**完全事前定义、不随现场调整、与概率场解耦**（测线布局不依据概率场优化）。
- **Leader = 地图窗口生成器**：sim 内 Leader **不是实时声呐模拟器**，不在 episode 中"检测"目标。它沿 lawnmower 滑动，用"自身后方那块"在静态场上裁出当前**観測区域 R(t)**，相当于在静止概率场上滑动的 mask。窗口编码的是**观测资格/物理约束**（Follower 只能去 Leader 后方、已扫过、安全的水域），不是信息可得性。
  - 观测区域设在后方的三重理由：① 需确认的目标本就在 Leader 身后（已扫过才生成概率）；② 不干扰 Leader 正在进行的探查；③ 避免 Follower 搅动底质/声学干扰污染探查。

---

## 4. Follower 与传感器

- **数量 N = 2，严格固定**（修士阶段）。忽略 N 的可扩展性研究，但 sim 保留 N 的修改接口。
- **相机传感器模型**定义在 Follower 类中，留修改接口，包含：
  - 检出率 p_d（高）、虚警率 p_fa（小）—— 取"接近完美但不完美"，为博士版 false-positive 修正留出意义空间。
  - footprint 几何（含横向宽度）。
- **初始能量**：作为初始条件 / 初始状态空间的一部分。
- **Follower 状态**：位置、电量（已耗/剩余）、**承诺队列**（已分配未走完的 waypoint 序列）及由其解析得到的剩余执行距离 / 预计可用时间。

---

## 5. 单步循环（方案 A：滚动反应式）

1. （episode 前，离线）数据处理从 MBES/SSS 可疑目标 → graded p(x,y)。
2. Leader 据自身位置估计设定后方观测区域 R(t)，随前进更新。
3. Leader 整合 R(t) 内概率场（降采样到粗网格）+ 两台 Follower 位置/电量/承诺队列 → state。
4. agent 决策 → 为**当前空闲的那一台** Follower 输出**一个**粗网格动作。
5. 映射模块将粗网格动作**纯几何映射**为世界坐标系下的 waypoint 坐标。
6. 模拟声学延迟/丢包（仅指令方向 Leader→Follower；**仅保留接口，训练时关闭**），Follower 收指令后依靠各自定位/导航依次前往。

---

## 6. 决策机制：动作格式与时机

### 6.1 动作格式（已锁）
- **离散粗网格 softmax**：动作 = 在观测区域降采样后的**粗网格**中选**一个 cell**。
  - 不采用连续 xy 回归：信息价值地形多峰，高斯策略难表达多峰决策且易塌缩；离散 softmax 天生表达多峰，且与走廊/熵降的栅格结算自洽。
  - 粗网格压到可控规模（约 10×20 ~ 20×40 量级，动作维度数百以内即可），修士只验证机制，粗网格足够。
  - 候选策略：**先用方案 i（全窗口固定粗网格）**；若动作空间过大再切方案 ii（局部邻域候选，仿 Vashisth）。中间隔一层映射模块，切换不影响 env 其余部分。
- **映射模块 = 纯几何映射**：仅把粗网格 cell 索引映射为该 cell 中心的真实世界坐标 waypoint。不做可达性检查、避障、吸附等任何额外规划。
- **一次只为一台 Follower 决策**（one-at-a-time）：与事件驱动时机咬合；天然兼容将来放开的 N。
- **每次决策只输出一个 waypoint**（非整条路径）：与滚动反应式自洽。

### 6.2 决策时机（已锁）
- **事件驱动**，非固定 Δt：当某台 Follower 到达其当前 waypoint（或 Leader 窗口推进到阈值）时，才触发一次决策，且只为刚空闲的那台决策。
- 忙着执行的 Follower 当拍不参与决策 ⇒ 天然处理"忙/暂时不可用"，且与 one-at-a-time 完美对应。
- ⚠ 代价：env 的 step() 比固定节拍复杂。为"贴合满血版"主动接受。

### 6.3 参考定位（Vashisth, arXiv:2409.16967）
- 可借鉴：动作=在候选集合上选一个；候选可局部采样；协同可靠"把队友意图编码进 state"。
- **不照搬**：该工作为去中心化 CTDE、N 可变、3D+yaw、预算 B_i=B/N 平分，并用 augmented graph + 双 GP 猜测队友轨迹。本项目是**中心化 Leader 决策**、N=2、2D、**min-max 能耗均衡（非预算平分）**——Leader 同时看到两台 Follower 全部状态，协同天然实现，**无需 augmented graph 那套去中心化补偿机制**（简化红利）。

---

## 7. 观测价值 = 信息熵的降低

- **价值定义**：目標分布確率マップ的**信息熵降低**，字面对应"消解底栖生物聚集的声学探测固有模糊性"。修士版计算**先验期望熵降 / 互信息**。
- **前置条件**：
  1. 必须显式定义相机传感器模型（p_d、p_fa、footprint）——否则熵降欠定。取"接近完美但不完美"。
  2. 数据处理输出 **graded** 概率场（已锁）——若二值化则全图熵本就极低，熵降退化为空壳。
  3. 开环下的"不重复计数"靠价值函数内部维护的"已计划观测覆盖"场，只记**哪些 cell 被（计划）拍过**（计划确定、确定性），**不记测量结果**（那需闭环）。递减收益活在 coverage 记账里，对外地图仍静止 ⇒ 不破开环。
- **结算时点**：在 Follower **物理到达并拍照那一刻**结算，非分配那一刻。
- **走廊结算（相机一路在拍）**：
  - 结算单元 = 路径线段扫过的格集合（非端点格）。把"上一 waypoint → 当前 waypoint"线段按走廊宽度扩成带，落栅格得覆盖格集，对其中**尚未被任何 Follower 计划覆盖过**的格累加期望熵降。
  - **走廊宽度 = 相机 footprint 横向宽度 × 0.9**（保守冗余：宁可少算覆盖，不虚报）。
  - 不重复计数对走廊同样适用（信息熵奖励形式天然支持）。
- **永久错过**：仅针对"滑出窗口前**从未被分配**"的高概率区域。已分配但未到达的 waypoint 照常执行、照常计入价值（哪怕区域已滑出窗口）。
- ⚠ 运营取舍：纯熵/互信息会把 Follower 优先派往 p≈0.5 的最模糊 cell（而非 p≈0.9 的最可能目标）。鉴于研究动机本就是"声学模糊"，把资源投向模糊区正是所需，与叙事自洽。

---

## 8. 优化目标与 Reward

- **结构**：**价值为目标、均衡为惩罚**。
  - 价值项作用于**总量**（期望熵降总和）。
  - 均衡项作用于**跨 Follower 能耗分布**（min-max 或能耗离散度），以惩罚形式进入。
- **均衡与效率分离**：须在覆盖量可比的前提下偏好 max_i E_i 最小的分配，否则单纯压低 max 会退化成"少干活"。
- **"均衡 on/off" 消融** = 均衡惩罚项的开/关。
- ⚠ 量纲：熵降（bit/nat）与能耗（J 或代理量）加权求和，权重需标定。
- **通信**：声学延迟/丢包**仅保留代码编辑接口**，训练 reward 干净（训练时延迟=0、无丢包）。

---

## 9. 能耗模型

- **Level 1 = 路径长度 + 转弯惩罚**（转角越大代价越高）。与"高机动转弯能耗浪费"研究线一致。
- 预留 **speed-power 标定接口**，对接第②层真机口径（推力指令积分 + 速度-功率标定）。
- 后续可升级 Level 2（沿运动学轨迹积分 speed-power 曲线）。

---

## 10. 时间 / 可用性建模

在修士静态开环设定下，以下两量为**确定性可解析量**（非"估计"，论文措辞应写"由承诺队列解析计算"）：

- **剩余执行距离**：d_i_rem = ‖f_i − w_1‖ + Σ_k ‖w_k − w_{k+1}‖（当前位置到队列首点，再累加队列内相邻点距离）。
- **预计可用时间**：t_i_avail = d_i_rem / v_i（+ 每 waypoint 停留拍照时间 × 剩余点数）（+ 声学延迟，默认 0）。
- ⚠ **转弯不计入时间**（只进能耗）。能耗模型认为转弯费力、时间模型当转弯不费时——有意的内部简化，换取事件驱动触发时刻计算干净。已接受。
- 喂入 state 前需归一化（d 用窗口尺度、t 用最大任务时间）。

---

## 11. 评估

- **指标集**：max（即 min-max 目标本身）+ CV 或 Jain（二选一，因 J=1/(1+CV²) 互为单调变换，不并列）+ 总能耗（揭示均衡 vs 效率 trade-off）+ **推理时间**（支撑"learned amortized policy + 资源受限部署"的核心卖点）。
- **消融维度**：**balance on/off**（+ 可能的概率场难度/簇结构）。**不含 N 扫描**（修士锁 N=2）。
- **基线**：
  - min-max VRP 强基线（OR-Tools / LKH，措辞称"近似最优"，非"ground truth"——min-max VRP 为 NP-hard）。
  - 小实例 MILP 精确解（PuLP / CP-SAT）作为真正的 ground truth。
- ⚠ 卖点侧重：修士第①层主要卖点为"**informative 价值（熵降）+ 资源受限可部署**"，均衡作为被验证的次要机制。需预期 reviewer 提问"N=2 的 min-max 与简单平分有何区别"。

---

## 12. 三层验证（整体研究计划，本文档聚焦第①层）

1. **第①层 — 仿真消融**（本 simulation 项目）：balance on/off，指标如上。
2. **第②层 — 单 Follower 顺序复现**：用 1 台真实 Follower 依次执行每个角色的规划路径，获得真实多机器人能耗分布（静态开环结构 ⇒ 执行期无机间交互 ⇒ 顺序复现有效）。
3. **第③层 — 端到端指令闭环 demo**：完整 Leader→Follower 指令 + 到达确认；声学延迟/丢包在此层最有意义。

硬件约束：1 Leader + 1 Follower；Leader 算力约 Pi 5 级；无电流计（用推力指令积分 + 速度-功率标定作代理）。

---

## 13. 训练实例设计

- 用**带簇结构的合成实例**训练（可调簇数/簇内密度），贴合底栖生物聚集的空间统计；避免均匀随机点（与真实统计不符）或仅在单张真实 map 上过拟合。
- 在 held-out 合成实例 + 真实 map 上评估。
- 真实 map 物理尺度百米级（200 m × 100 m）。

---

## 附：与既往研究的关键事实校正（供论文引用核对）

- HIMOS = Chen et al., arXiv:2603.08336（2026，单 AUV，仅仿真）。
- Vashisth = arXiv:2409.16967（多机器人 target mapping IPP）；"noiseless classifier"假设属 Vashisth，**不**属 HIMOS。
- Westheider = IROS 2023（多 UAV 自适应 IPP），与 Vashisth 为两篇不同论文。
- "McCammon & Hollinger ICRA 2018" **不存在**；异构海洋机器人团队正确引用为 McCammon et al., JFR 2021。
- 机间能耗/工作量均衡有成熟先行工作（Chen & Zhu IEEE TCDS；进化/PSO 方法）；本项目（A）的新颖性是**方法论层面**（learned amortized policy、资源受限部署），**非**"首次做能耗均衡"。
