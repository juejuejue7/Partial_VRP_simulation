"""[A0 独占] 冻结契约层 —— 签名 + 类型 + 口径,尽量不含实现。

硬纪律(CLAUDE.md):
- 对契约编程,不对实现编程:跨模块只依赖本目录的签名。
- 改接口必须经 A0:发现契约不够用,提交裁决并广播,禁止自行扩展。
- 本目录只有 A0 可写;其它 agent 只读。

裁决记录见 DECISIONS.md;可复用的 msim 符号白名单见 REUSE_FROM_MSIM.md。
"""
